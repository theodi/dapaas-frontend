<head>
  <title>DaPaaS <%= strip_tags('| ' + yield(:title)).strip if yield(:title).present? %><%= " | " + yield(:page_title) if content_for?(:page_title) %></title>
  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
</head>

<body>

  <div id='wrapper'>
    <% if content_for?(:title) %>
      <header class="page-title">
          <div class="container">
              <h1><%= yield :title %></h1>
          </div>
      </header>
    <% end %>

    <%= yield :hero %>

    <div class="container">
        <div class="row">
          <%= yield %>
        </div>
    </div>

  </div>

</body>
